# Optional. Without this (or with --demo), the crew runs in DEMO MODE:
# planning uses a fixed pipeline shape, research uses direct local keyword
# search, writing uses a template, and review uses rule-based checks.
# With a real key, every agent is powered by Claude — including a real
# tool-use round trip for the researcher agent.
ANTHROPIC_API_KEY=
